<%= form_tag "/results", :method => "get" do %>


<p class="tk-museo-slab"> Hi! </p>
<p class="tk-museo-slab"> I am a <%= select_tag "jobs", options_for_select(@jobs), :include_blank => true %>.</p>
<p class="tk-museo-slab"> I am moving to <%= select_tag "cities", options_for_select(@cities), :include_blank => true %>.</p>
<p class="tk-museo-slab"> What should I do there? </h1>
<p class="tk-museo-slab"> Love, Me</p>

<h2><%= link_to "Want to help people moving to your city? Submit your own advice document.", Koala::Facebook::OAuth.new.url_for_oauth_code(:callback => oauth_url) %></h2>

<% end -%>

<script type="text/javascript">
	$(function(){
		$("select").change(function(){
			var navigate = true;
				$("select").each(function(){
					if(this.value===''){
						navigate = false;
					}
				})
				if (navigate === true){
					$("form").submit();
				}
		})
	});
</script>